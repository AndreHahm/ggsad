---
phase: 01-normative-clarification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/method/GG-SAD_normative_method_specification.md
  - .planning/phases/01-normative-clarification/01-BASELINE.txt
autonomous: true
requirements: [NORM-01, NORM-02]

estimate:
  tokens: 42000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Before any spec edit lands, `.planning/phases/01-normative-clarification/01-BASELINE.txt` contains a single git commit SHA captured via `git rev-parse HEAD`, giving every later plan/phase verification in this phase a durable, non-hard-coded anchor that survives per-task commits and separate plan invocations (F-01)."
    - "Reading the new Section 1.1, a reviewer can identify which of the four NORM-01 categories (Authority & Applicability, Method Semantics, Reference-Implementation Requirements, Optional Integration Guidance) every existing top-level section (1 through 21) belongs to."
    - "Reading Section 4.3, a reviewer can tell exactly which artifact files are mandatory only when persistent state is used versus always conditional, without ambiguity."
  artifacts:
    - ".planning/phases/01-normative-clarification/01-BASELINE.txt — single-line pre-execution HEAD SHA, committed before any other change in this phase; consumed by this plan's own closing verification and by Plans 01-02 and 01-03's baseline-anchored diff checks"
    - "docs/method/GG-SAD_normative_method_specification.md — new ### 1.1 Document Scope and Category Map subsection with a 21-row category-mapping table"
    - "docs/method/GG-SAD_normative_method_specification.md — rewritten ### 4.3 Change-Specific Documents subsection (adds state.yaml and review.md to the artifact tree; defines mandatory-information vs. mandatory-file distinction)"
  key_links:
    - "Task 1's baseline commit MUST land before Task 2 or Task 3 touch the spec file, and MUST NOT be re-created by Plan 01-02 or Plan 01-03 — both later plans read this same committed file rather than recomputing or hard-coding a SHA."
    - "Section 1.1's category-mapping table is completed here for the 21 sections that exist today; Plan 01-03 (NORM-06) adds a brand-new ## 22 section and MUST append one more row to this same table in the same task that creates Section 22, or the table silently goes stale."
    - "Section 4.3's new state.yaml/review.md content must stay consistent with Plan 01-02's Section 8 rewrite (NORM-03), which is the other place `closed`/status semantics are defined — this plan does not touch status semantics, only artifact-file conditionality."
---

<objective>
Establish this phase's durable pre-execution baseline (Task 1), then produce the first slice of the Phase 1 proposed diff to `docs/method/GG-SAD_normative_method_specification.md`: an explicit document-category map that separates the SPEC's own authority/applicability from method semantics, reference-implementation requirements, and optional integration guidance (NORM-01), and a canonical artifact model that distinguishes mandatory information from mandatory files (NORM-02). The two spec edits are additive/localized — no existing top-level section is renumbered, reordered, or removed.

This plan produces the PROPOSED content of the diff. The edit lands directly on the live spec file (per D-01, direct-edit-in-place), but the diff is not final, approved, or merged until Phase 2 (Owner Approval) and Phase 3 (Independent Review) — separate later phases this session has not authorized — complete.

Per checker finding F-01, this plan opens by recording the exact pre-phase `git rev-parse HEAD` SHA into a committed, canonical Phase 1 planning artifact, `.planning/phases/01-normative-clarification/01-BASELINE.txt`, before any other change lands. Every later verification in this plan, and in Plans 01-02 and 01-03 (separate executor invocations), diffs against this same committed file instead of a bare, unanchored `git diff` — so "nothing outside the target file changed" stays provable across per-task commits and across plan boundaries, without hard-coding a SHA into any plan.

Purpose: give the repository owner one coherent, minimal-diff artifact that answers "what kind of statement is each section of this spec making" and "what exactly is a compliant change record required to contain" — anchored to a verifiable, non-hard-coded baseline.
Output: `.planning/phases/01-normative-clarification/01-BASELINE.txt` (new baseline artifact) and `docs/method/GG-SAD_normative_method_specification.md` with a new `### 1.1 Document Scope and Category Map` subsection and a rewritten `### 4.3 Change-Specific Documents` subsection.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<shell_contract>
All `<verify>` and `<acceptance_criteria>` commands in this plan are POSIX-shell (bash) commands, run via the project's Bash tool — which executes Git Bash on this Windows repository, not `cmd.exe` or PowerShell. `git`, `grep`, `sed`, `awk`, and bash process substitution (`<(...)`) are available through that shell. Commands avoid `/tmp` and any other host-specific path; where a scratch comparison is needed, use process substitution or a path under this phase's own `.planning/` directory instead. Every "no unrelated files changed" check reads the baseline SHA from `.planning/phases/01-normative-clarification/01-BASELINE.txt` (created by Task 1) rather than hard-coding a commit hash.
</shell_contract>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-normative-clarification/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Record Phase 1 pre-execution baseline (F-01)</name>
  <read_first>
    .planning/phases/01-normative-clarification/01-CONTEXT.md (D-01: the diff lands directly on the live spec file; this task runs before that edit begins)
  </read_first>
  <files>.planning/phases/01-normative-clarification/01-BASELINE.txt</files>
  <action>
    Before making any other change in this plan or phase, capture the repository's current HEAD commit with `git rev-parse HEAD` and write exactly that 40-character SHA, and nothing else (single line, no trailing prose), to a new file `.planning/phases/01-normative-clarification/01-BASELINE.txt`. Commit only this one file in its own commit, with a message identifying it as the Phase 1 pre-execution baseline (e.g. `chore(01): record phase 1 pre-execution baseline`), before starting Task 2. Do not touch `docs/method/GG-SAD_normative_method_specification.md` or any other file in this task.

    This baseline is the durable, non-hard-coded anchor every later verification in this plan, and in Plans 01-02 and 01-03 (each a separate executor invocation), diffs against — `git diff --name-only "$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)"..HEAD` — instead of a bare, unanchored `git diff` that would compare only the working tree to the index and prove nothing once the executor has already auto-committed. Plans 01-02 and 01-03 MUST read this committed file rather than recomputing or hard-coding a SHA of their own.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-normative-clarification/01-BASELINE.txt && grep -cE '^[0-9a-f]{40}$' .planning/phases/01-normative-clarification/01-BASELINE.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-normative-clarification/01-BASELINE.txt` exists and contains exactly one line matching a 40-character hex SHA: `grep -cE '^[0-9a-f]{40}$' .planning/phases/01-normative-clarification/01-BASELINE.txt` returns exactly 1
    - The recorded SHA resolves to a real, existing commit: `git cat-file -e "$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)"^{commit}` exits 0
    - The commit that added this file touches only this one path — a commit-aware check against the task's own commit, not a bare working-tree diff: `git diff-tree --no-commit-id --name-only -r HEAD` returns exactly `.planning/phases/01-normative-clarification/01-BASELINE.txt`
    - `docs/method/GG-SAD_normative_method_specification.md` is not yet modified relative to the recorded baseline: `git diff --name-only "$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)"..HEAD -- docs/method/GG-SAD_normative_method_specification.md` produces no output
  </acceptance_criteria>
  <done>`.planning/phases/01-normative-clarification/01-BASELINE.txt` exists, contains a single valid pre-phase commit SHA, is committed in its own commit touching only that file, and no spec edit has landed yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Section 1.1 Document Scope and Category Map (NORM-01, per D-02)</name>
  <read_first>
    docs/method/GG-SAD_normative_method_specification.md (full file — needed to enumerate every existing top-level "## N." heading accurately before categorizing it)
    .planning/phases/01-normative-clarification/01-CONTEXT.md (D-02: keep current section order/numbering; use category framing or a mapping table, not new top-level Part/Section groupings)
  </read_first>
  <files>docs/method/GG-SAD_normative_method_specification.md</files>
  <action>
    Insert a new subsection `### 1.1 Document Scope and Category Map` immediately after the existing prose of `## 1. Purpose` and before `## 2. Normative Terms`. Per D-02, do not introduce a new top-level Part/Section grouping and do not renumber any existing "## N." heading — this is a within-Section-1 addition only, and Section 1 currently has no subsections, so adding 1.1 disturbs no other section's numbering.

    The new subsection must contain exactly two elements:

    (a) One paragraph stating that this specification is the leading GG-SAD semantic and product baseline, and clarifying that the precedence list in Section 4 ("Document Hierarchy") governs artifacts inside a GG-SAD-managed project — it does not rank this specification itself, which remains superior to every document that list orders.

    (b) A Markdown table with columns "Section" and "Category" listing every existing top-level section by its exact current number and title (Sections 1 through 21), tagging each with exactly one of four category values: "Authority & Applicability", "Method Semantics", "Reference-Implementation Requirements", "Optional Integration Guidance". Use these category assignments, unchanged: Section 1 (Purpose) = Authority & Applicability. Sections 2 (Normative Terms), 3 (Core Principles), 4 (Document Hierarchy), 5 (Workflow and Compliance Tailoring), 6 (Size Classes), 7 (Phase Model), 8 (State Model), 9 (Definition of Ready), 10 (Definition of Done), 11 (Definition of Wait), 12 (Definition of Fail), 13 (Conflict and Decision Rules), 14 (Pair Review Model), 15 (Evidence Model), 20 (Completion Criteria for a Change), 21 (Quick Reference) = Method Semantics. Sections 16 (Minimum Templates), 19 (Agent Execution Algorithm) = Reference-Implementation Requirements. Sections 17 (Combination Contracts), 18 (GG-SAD Memory Model) = Optional Integration Guidance.

    Do not modify Section 4.1's precedence list or any other existing section's content in this task.
  </action>
  <verify>
    <automated>grep -c '^### 1.1 Document Scope and Category Map' docs/method/GG-SAD_normative_method_specification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### 1.1 Document Scope and Category Map' docs/method/GG-SAD_normative_method_specification.md` returns exactly 1
    - `awk '/^### 1.1 Document Scope and Category Map/,/^## 2\. Normative Terms/' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^\| [0-9]+'` returns exactly 21 (one data row per existing top-level section)
    - `grep -cE '^## [0-9]+\.' docs/method/GG-SAD_normative_method_specification.md` still returns exactly 21 (no top-level section added, removed, or renumbered by this task)
    - each of the four category labels ("Authority & Applicability", "Method Semantics", "Reference-Implementation Requirements", "Optional Integration Guidance") appears at least once in the new table: `grep -c 'Authority & Applicability' docs/method/GG-SAD_normative_method_specification.md` >= 1, `grep -c 'Reference-Implementation Requirements' docs/method/GG-SAD_normative_method_specification.md` >= 1, `grep -c 'Optional Integration Guidance' docs/method/GG-SAD_normative_method_specification.md` >= 1
  </acceptance_criteria>
  <done>Section 1.1 exists immediately after Section 1's prose, contains a 21-row category-mapping table covering every existing top-level section, and no existing section was renumbered or reordered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite Section 4.3 as the canonical artifact model (NORM-02)</name>
  <read_first>
    docs/method/GG-SAD_normative_method_specification.md — Section 4 region (`### 4.1 Binding Order of Precedence` through `## 5. Workflow and Compliance Tailoring`), already loaded from Task 2's full-file read
    .planning/REQUIREMENTS.md — NORM-02 line
  </read_first>
  <files>docs/method/GG-SAD_normative_method_specification.md</files>
  <action>
    Rewrite `### 4.3 Change-Specific Documents` in place. Leave `### 4.1 Binding Order of Precedence` and `### 4.2 Project-Wide Documents` completely unchanged.

    Replace the existing artifact-tree code block (which currently lists only spec.md, plan.md, tasks.md, evidence.md) with one that also lists `state.yaml` and `review.md` alongside the four existing files, under `specs/<change-id>/`.

    Replace the two-sentence note that follows the tree with explicit canonical-artifact-model prose covering, in order: (1) `spec.md` remains the mandatory file for normal (Class M/L) changes per Section 6; (2) every change carries mandatory INFORMATION regardless of file layout — goal, specification anchor, gate outcome, evidence, wait/fail behavior (when applicable), and final status; (3) `state.yaml` is a mandatory FILE only when the project uses persistent, machine-readable workflow state to track phase, status, gates, and history — when state is not persisted that way, the same mandatory information may instead be recorded inline in `spec.md` or `evidence.md`; (4) `plan.md`, `tasks.md`, `evidence.md`, and `review.md` remain conditional files, required only when size, risk, evidence, or review-depth needs call for them; (5) inline storage of any of the above information is permitted only when its authoritative location and required fields are unambiguous.
  </action>
  <verify>
    <automated>grep -c 'state.yaml' docs/method/GG-SAD_normative_method_specification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'state.yaml' docs/method/GG-SAD_normative_method_specification.md` returns at least 1 (0 before this task)
    - `grep -c 'review.md' docs/method/GG-SAD_normative_method_specification.md` returns at least 2 (1 existing mention in Section 14.5, now also referenced in the rewritten 4.3 tree)
    - `grep -ci 'mandatory information' docs/method/GG-SAD_normative_method_specification.md` returns at least 1
    - `grep -ci 'mandatory file' docs/method/GG-SAD_normative_method_specification.md` returns at least 1
    - Section 4.1 is byte-identical to its pre-task state: `sed -n '/^### 4.1 Binding Order of Precedence/,/^### 4.2 Project-Wide Documents/p' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^[0-9]+\. '` returns exactly 10 (the precedence list's item count is unchanged)
  </acceptance_criteria>
  <done>Section 4.3 defines the canonical artifact model with an explicit mandatory-information-versus-mandatory-file distinction, includes state.yaml and review.md in the artifact tree, and Sections 4.1/4.2 are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> leading normative authority file | An AI agent edits the single leading document-authority file for the whole GG-SAD product directly; the edit is provisional until Phase 2/3 gates close. |
| This phase's diff -> Phase 2 (Owner Approval) / Phase 3 (Independent Review) | Content produced across all of Phase 1's plans is not treated as final or mergeable until both hard gates complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | docs/method/GG-SAD_normative_method_specification.md | high | mitigate | Each task's `<files>` names only the target spec file; the phase's diff is not treated as final/merged until Phase 2 (APPR-01/02) explicitly approves the exact diff and Phase 3 (REVIEW-01..03) resolves blocking findings. |
| T-01-02 | Repudiation | git commit history for this file | medium | mitigate | The commit for this plan cites NORM-01/NORM-02 and D-01/D-02 explicitly, giving Phase 2/3 reviewers a traceable authorship/rationale record. |
| T-01-03 | Tampering | src/ggsad/ (out-of-scope directory) | high | mitigate | This plan's only `<files>` entries are the normative spec file and the baseline artifact; no task in this plan reads or writes anything under src/ggsad/, and Plan 01-03's Task 3 runs a phase-wide, baseline-anchored diff check (`git diff --name-only $(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)..HEAD`, filtered to allow only `.planning/` paths) enforcing NORM-08 before the phase closes. |
| T-01-04 | Information Disclosure | normative spec content | low | accept | The specification is already a public, non-secret governance document; this plan introduces no credentials, PII, or internal-only data. |
| T-01-05 | Tampering | .planning/phases/01-normative-clarification/01-BASELINE.txt | medium | mitigate | The baseline file is created and committed by Task 1, before any other change in the phase, in its own commit verified by a commit-aware `git diff-tree` check (not a bare working-tree diff); every later baseline-anchored check in this phase reads this one committed file instead of a hard-coded SHA. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install tasks exist in this plan (documentation-only diff); the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `grep -c '^### 1.1 Document Scope and Category Map' docs/method/GG-SAD_normative_method_specification.md` == 1
- `awk '/^### 1.1 Document Scope and Category Map/,/^## 2\. Normative Terms/' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^\| [0-9]+'` == 21
- `grep -cE '^## [0-9]+\.' docs/method/GG-SAD_normative_method_specification.md` == 21
- `grep -c 'state.yaml' docs/method/GG-SAD_normative_method_specification.md` >= 1
- `grep -c 'review.md' docs/method/GG-SAD_normative_method_specification.md` >= 2
- `.planning/phases/01-normative-clarification/01-BASELINE.txt` exists and `BASE=$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)` resolves to a real commit (`git cat-file -e "$BASE"^{commit}` exits 0)
- Baseline-anchored, non-`.planning/` diff since `$BASE` is exactly one path: `git diff --name-only "$BASE"..HEAD | grep -v '^\.planning/'` returns exactly `docs/method/GG-SAD_normative_method_specification.md`
- No product path changed since `$BASE`: `git diff --name-only "$BASE"..HEAD -- src/ggsad/` is empty, `git diff --name-only "$BASE"..HEAD -- tests/` is empty, `git diff --name-only "$BASE"..HEAD -- .ggsad/schemas/` is empty, and `git diff --name-only "$BASE"..HEAD | grep -c '_DE\.md$'` returns 0
</verification>

<success_criteria>
- The Phase 1 pre-execution baseline is recorded in `.planning/phases/01-normative-clarification/01-BASELINE.txt` before any spec edit lands, giving every later check in this phase a durable, non-hard-coded anchor (F-01 resolved).
- Section 1.1 exists with a complete, accurate 21-row category map (NORM-01 satisfied for this plan's scope).
- Section 4.3 defines the canonical artifact model with the mandatory-information-vs-mandatory-file distinction and includes state.yaml/review.md (NORM-02 satisfied).
- No file under src/ggsad/ was read for modification or modified (NORM-08 respected).
- No existing top-level section was renumbered, reordered, or removed.
</success_criteria>

<output>
Create `.planning/phases/01-normative-clarification/01-01-SUMMARY.md` when done
</output>
