---
phase: 01-normative-clarification
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/method/GG-SAD_normative_method_specification.md
autonomous: true
requirements: [NORM-06, NORM-07, NORM-08]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Reading the new Section 22, an independent implementer can identify the minimal result envelope's exact field set and presence rules — `operation`/`result`/`changed` present in every response regardless of outcome, `state` included when applicable, `issues` included on rejected/error results and omitted on success, `data` always optional — without needing GG-SAD's own Python reference implementation or any specific vendor tool."
    - "Every subsection number under Sections 6, 7, 9, 10, 11, 12, and 13 matches its parent top-level section number; Section 8 (fixed in Plan 01-02) and Section 22 (created in this plan) are already correctly numbered."
    - "No commercial AI product name remains cited in Section 14.2; its example list is explicitly labeled illustrative and non-normative."
    - "The whole phase's diff, verified against the pre-phase baseline Plan 01-01's Task 1 committed (not a bare or hard-coded `git diff`), touches exactly docs/method/GG-SAD_normative_method_specification.md and no path under src/ggsad/, tests/, or .ggsad/schemas/, alongside only canonical `.planning/` artifacts."
  artifacts:
    - "docs/method/GG-SAD_normative_method_specification.md — new ## 22. Minimal Automation Contract section (22.1-22.3)"
    - "docs/method/GG-SAD_normative_method_specification.md — ### 1.1 Document Scope and Category Map table updated with one new row for Section 22"
    - "docs/method/GG-SAD_normative_method_specification.md — subsection numbers under Sections 6, 7, 9, 10, 11, 12, 13 mechanically repaired to match their parent section number"
    - "docs/method/GG-SAD_normative_method_specification.md — ### 14.2 Participants and Identity genericized (tool-local citations removed, example list labeled non-normative)"
  key_links:
    - "Task 1 must add the Section 22 table row to Section 1.1 in the same task that creates Section 22 — otherwise the category map (created in Plan 01-01) goes stale relative to the actual top-level section count."
    - "Task 2's renumbering must NOT re-touch Section 8 (already correctly numbered by Plan 01-02) or Section 22 (already correctly numbered by this plan's Task 1) — its scope is exactly Sections 6, 7, 9, 10, 11, 12, 13."
    - "Task 3's final verification depends on Task 1 and Task 2 both having landed in this same plan, since it checks the whole-file section-numbering and category-map consistency they jointly produce."
    - "Task 3's closing verification reads the Phase 1 baseline recorded by Plan 01-01's Task 1 (`.planning/phases/01-normative-clarification/01-BASELINE.txt`) — a committed, non-hard-coded SHA — rather than using a bare `git diff` (F-01)."
---

<objective>
Complete the Phase 1 proposed diff to `docs/method/GG-SAD_normative_method_specification.md`: define a technology-neutral minimal automation contract (NORM-06, per D-04's narrower envelope), repair document quality by mechanically renumbering the remaining out-of-sync subsections and removing tool-local citations (NORM-07), and run a final phase-closing verification confirming no file under `src/ggsad/` was touched (NORM-08).

This is Wave 3, the final wave of the Phase 1 diff (depends on Plan 01-02's Section 8/20/5.4/14.5 edits, since all three plans touch the same file). The edit lands directly on the live spec file (per D-01); it remains provisional until Phase 2 (Owner Approval) and Phase 3 (Independent Review) — separate later phases not authorized in this session — complete.

Per checker finding F-01, Task 3's phase-closing verification is commit-aware and baseline-anchored: it reads the pre-phase SHA that Plan 01-01's Task 1 already committed to `.planning/phases/01-normative-clarification/01-BASELINE.txt` and diffs against it, rather than a bare `git diff` that would compare only the working tree to the index and prove nothing once the executor has auto-committed. This plan does not recreate or hard-code that baseline — it consumes the one file Plan 01-01 already produced. Per checker finding F-02, Task 1's Section 22.2 envelope definition states exact, non-uniform presence rules for each field rather than claiming all six always appear.

Purpose: close out the diff with a self-contained, vendor-neutral automation contract and a document that is internally consistent (correct section numbers, no dangling non-English-file citation, no vendor-tool citations, examples clearly labeled) and ready for Phase 2 review.
Output: `docs/method/GG-SAD_normative_method_specification.md` with a new Section 22, a corrected Section 1.1 table, mechanically repaired subsection numbers, a genericized Section 14.2, and a phase-closing verification record.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<shell_contract>
All `<verify>` and `<acceptance_criteria>` commands in this plan are POSIX-shell (bash) commands, run via the project's Bash tool — which executes Git Bash on this Windows repository, not `cmd.exe` or PowerShell. `git`, `grep`, `sed`, `awk`, and bash process substitution (`<(...)`) are available through that shell. Commands avoid `/tmp` and any other host-specific path — Task 3's composite check uses process substitution instead of a scratch file. Every "no unrelated files changed" check reads the baseline SHA from `.planning/phases/01-normative-clarification/01-BASELINE.txt` (created by Plan 01-01's Task 1, read here rather than recreated) instead of hard-coding a commit hash.
</shell_contract>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-normative-clarification/01-CONTEXT.md
@.planning/phases/01-normative-clarification/01-BASELINE.txt
@.ggsad/schemas/state.schema.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Section 22 Minimal Automation Contract and update the Section 1.1 category map (NORM-06, D-04)</name>
  <reversibility rating="costly">Phase 6 (Implementation Conformance Audit) and Phase 7 (Gap Remediation) will scope their evidenced-gap and remediation work against this exact field set; changing it after Phase 2/3 approval requires re-approval, re-review, and replanning those phases' scope — carried forward from CONTEXT.md D-04.</reversibility>
  <read_first>
    docs/method/GG-SAD_normative_method_specification.md — `## 21. Quick Reference` through end of file, and `### 1.1 Document Scope and Category Map` (created by Plan 01-01)
    .ggsad/schemas/state.schema.json — `flow.phase`/`flow.status` enums (shape of the envelope's "state" field)
    .planning/phases/01-normative-clarification/01-CONTEXT.md — D-04 (exact envelope field list)
  </read_first>
  <files>docs/method/GG-SAD_normative_method_specification.md</files>
  <action>
    Append a new top-level section `## 22. Minimal Automation Contract` at the true end of the file, after Section 21's closing sentence ("GG-SAD does not optimize for maximum documentation volume..."). Give it exactly three subsections: `### 22.1 Contract Operations`, `### 22.2 Minimal Result Envelope`, `### 22.3 Rejection and Output Requirements`.

    22.1 Contract Operations: list the four technology-neutral operations a GG-SAD automation contract must support: initializing a stand-alone GG-SAD project; creating a goal-bound change; validating governing configuration, artifacts, references, and state; evaluating and executing one controlled state transition, per the Section 8 Transition Table. State the contract specifies observable behavior and compatibility requirements only — not any specific programming language, module structure, or vendor-specific agent interface.

    22.2 Minimal Result Envelope: state that every contract operation MUST return a result envelope with three always-present fields — `operation` (which contract operation ran), `result` (one of `success`, `rejected`, or `error`, never partial), and `changed` (whether persistent state changed) — present in every response regardless of outcome. Two further fields are conditionally present: `state` (the resulting phase/status; per Section 8, phase includes closed and status carries the terminal outcome) is included when applicable to the operation, and omitted when the operation produces no phase/status result; `issues` (stable codes plus human-readable messages) is included whenever `result` is `rejected` or `error` and omitted whenever `result` is `success`. One field, `data`, is always optional operation-specific output, present only when relevant to a specific operation. State explicitly that a goal summary, a specification anchor, a gate outcome, evidence references, and a timestamp are not universally required envelope fields — they may appear inside `data` when relevant to a specific operation. Do not describe the envelope as a fixed six-field object present in identical shape on every response — per this paragraph, the presence rule for `state`, `issues`, and `data` is conditional, not universal.

    22.3 Rejection and Output Requirements: state invalid operations must be rejected without any partial mutation of persisted state (result of rejected or error, changed of false); implementations must emit both an actionable human-readable message and the structured envelope for every operation; implementations must record transition history and relevant evidence per the Section 8 Transition Table and the Section 15 Evidence Model.

    In the same task, update `### 1.1 Document Scope and Category Map` (created by Plan 01-01) by appending one new table row for "22. Minimal Automation Contract" tagged "Reference-Implementation Requirements" — do not alter any of the existing 21 rows.
  </action>
  <verify>
    <automated>grep -c '^## 22\. Minimal Automation Contract' docs/method/GG-SAD_normative_method_specification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## 22\. Minimal Automation Contract' docs/method/GG-SAD_normative_method_specification.md` returns exactly 1
    - `grep -cE '^### 22\.[123] ' docs/method/GG-SAD_normative_method_specification.md` returns exactly 3
    - Scoped to the 22.2 subsection only (from its own heading up to 22.3's), the three unconditional field names are present, backtick-quoted: `awk '/^### 22\.2 /,/^### 22\.3 /' docs/method/GG-SAD_normative_method_specification.md | grep -c '`operation`'` >= 1, the same scoped extraction piped to `grep -c '`result`'` >= 1, and to `grep -c '`changed`'` >= 1
    - The same 22.2-scoped region states the unconditional framing verbatim: `awk '/^### 22\.2 /,/^### 22\.3 /' docs/method/GG-SAD_normative_method_specification.md | grep -c 'present in every response regardless of outcome'` >= 1
    - The same region states `state`'s conditional presence: `awk '/^### 22\.2 /,/^### 22\.3 /' docs/method/GG-SAD_normative_method_specification.md | grep -c 'included when applicable'` >= 1
    - The same region states `issues`'s deterministic conditional presence: `awk '/^### 22\.2 /,/^### 22\.3 /' docs/method/GG-SAD_normative_method_specification.md | grep -c 'included whenever.*rejected.*error'` >= 1, and the same scoped extraction piped to `grep -c 'omitted whenever.*success'` >= 1
    - The same region states `data`'s unconditional optionality: `awk '/^### 22\.2 /,/^### 22\.3 /' docs/method/GG-SAD_normative_method_specification.md | grep -c 'is always optional'` >= 1
    - No claim that the envelope always contains all six fields in identical, fixed shape: `grep -c 'all six fields' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -c 'goal_summary\|specification_anchor\|gate_outcome' docs/method/GG-SAD_normative_method_specification.md` returns 0 (these snake_case field names must not appear as required envelope fields)
    - `awk '/^### 1.1 Document Scope and Category Map/,/^## 2\. Normative Terms/' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^\| [0-9]+'` returns exactly 22 (was 21 after Plan 01-01)
    - `grep -cE '^## [0-9]+\.' docs/method/GG-SAD_normative_method_specification.md` returns exactly 22 (was 21 before this task)
  </acceptance_criteria>
  <done>Section 22 exists with the D-04 envelope's exact field set and precise presence rules (operation/result/changed unconditional; state included when applicable; issues included for rejected/error results and omitted on success; data always optional), and Section 1.1's category map is updated to include it in the same task.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mechanically renumber Sections 6/7/9/10/11/12/13 and genericize Section 14.2's tool citations (NORM-07)</name>
  <read_first>
    docs/method/GG-SAD_normative_method_specification.md — `## 6. Size Classes` through `## 14. Pair Review Model` (`### 14.2 Participants and Identity` specifically), and `## 17. Combination Contracts` (for the existing non-normative-labeling pattern to match)
    .planning/REQUIREMENTS.md — NORM-07 line
  </read_first>
  <files>docs/method/GG-SAD_normative_method_specification.md</files>
  <action>
    Part A — mechanical subsection renumbering. Section 8 is already correctly numbered by Plan 01-02's Task 1 and must not be touched here. Rename these subsection headers so each subsection number matches its parent top-level section number, changing only the number, never the title text or body content:
    - Section 6 "Size Classes": "### 5.1 Class S — Patch" becomes "### 6.1 Class S — Patch"; "### 5.2 Class M — Change" becomes "### 6.2 Class M — Change"; "### 5.3 Class L — Initiative" becomes "### 6.3 Class L — Initiative".
    - Section 7 "Phase Model": "### 6.1 Permitted Shortened Flows" becomes "### 7.1 Permitted Shortened Flows".
    - Section 9 "Definition of Ready": "### 8.1 Ready-to-Spec" through "### 8.5 Ready-to-Release" become "### 9.1" through "### 9.5" (same titles).
    - Section 10 "Definition of Done": "### 9.1 Spec-Done" through "### 9.5 Release-Done" become "### 10.1" through "### 10.5" (same titles).
    - Section 11 "Definition of Wait": "### 10.1 Typical Wait Categories" through "### 10.3 AI Agent Behavior in a Wait State" become "### 11.1" through "### 11.3" (same titles).
    - Section 12 "Definition of Fail": "### 11.1 Typical Fail Categories" and "### 11.2 Mandatory Content of a Fail Rule" become "### 12.1" and "### 12.2" (same titles).
    - Section 13 "Conflict and Decision Rules": "### 12.1 ADR Conflicts", "### 12.2 Specification Drift", "### 12.3 Breaking Changes" become "### 13.1", "### 13.2", "### 13.3" (same titles).

    Do not change any top-level "## N." heading number, any subsection title text, or any subsection under Sections 1 through 5, 8, or 14 through 22 — those were already correctly numbered before this task, or were already fixed by an earlier task in this phase. The one existing numeric cross-reference in the document — "the invariant core defined in Section 3.7" inside Section 5.2 Tailoring Dimensions — is outside the 6-13 range and must remain unchanged.

    Part B — genericize Section 14.2's example list. Replace the current example bullet list (which names specific commercial AI products) with role-generic participant-type labels only: Human, AI Agent (any vendor or product), External Review Service — covering the same five illustrative combinations already present (Human-Human, Human-AI Agent, AI Agent-AI Agent from a distinct vendor or product, AI Agent-Human, AI Agent-External Review Service). Precede the replacement list with this exact qualifier line: "Examples (illustrative, non-normative):" — matching the labeling pattern already used for the combination-contract example in Section 17. Do not alter Section 14.1, 14.3, or 14.4.

    Part C — verify the other-language sibling specification file is not cited. Confirm this file contains no citation of, or reference to, the non-English sibling specification document that shares this document's base filename. As of this task's own pre-edit audit, no such citation exists in the current file, so this is a verify-and-remove step, not a content-authoring step: if an earlier task in this phase inadvertently introduced such a citation, remove it before completing this task; otherwise no change is needed for Part C.
  </action>
  <verify>
    <automated>grep -c '^### 6.1 Class S' docs/method/GG-SAD_normative_method_specification.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### 6.1 Class S' docs/method/GG-SAD_normative_method_specification.md` == 1, `grep -c '^### 6.2 Class M' docs/method/GG-SAD_normative_method_specification.md` == 1, `grep -c '^### 6.3 Class L' docs/method/GG-SAD_normative_method_specification.md` == 1, `grep -c '^### 5.1 Class S' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -c '^### 7.1 Permitted Shortened Flows' docs/method/GG-SAD_normative_method_specification.md` == 1, `grep -c '^### 6.1 Permitted Shortened Flows' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^### 9\.[1-5] ' docs/method/GG-SAD_normative_method_specification.md` == 5, `grep -c '^### 8.1 Ready-to-Spec' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^### 10\.[1-5] ' docs/method/GG-SAD_normative_method_specification.md` == 5, `grep -c '^### 9.1 Spec-Done' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^### 11\.[1-3] ' docs/method/GG-SAD_normative_method_specification.md` == 3, `grep -c '^### 10.1 Typical Wait Categories' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^### 12\.[12] ' docs/method/GG-SAD_normative_method_specification.md` == 2, `grep -c '^### 11.1 Typical Fail Categories' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^### 13\.[123] ' docs/method/GG-SAD_normative_method_specification.md` == 3, `grep -c '^### 12.1 ADR Conflicts' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -F 'the invariant core defined in Section 3.7' docs/method/GG-SAD_normative_method_specification.md` still matches (unrelated cross-reference untouched)
    - `grep -c 'Claude Code' docs/method/GG-SAD_normative_method_specification.md` == 0, `grep -c 'CodeRabbit' docs/method/GG-SAD_normative_method_specification.md` == 0, `grep -cw 'Codex' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -c 'Examples (illustrative, non-normative):' docs/method/GG-SAD_normative_method_specification.md` >= 1
    - `grep -ciE 'german|deutsch|_DE\.md' docs/method/GG-SAD_normative_method_specification.md` == 0
    - `grep -cE '^## [0-9]+\.' docs/method/GG-SAD_normative_method_specification.md` == 22 (unchanged from Task 1's result — no top-level section added or removed by this task)
  </acceptance_criteria>
  <done>Subsections under Sections 6, 7, 9, 10, 11, 12, 13 match their parent section number; Section 14.2 no longer cites any commercial AI product and is explicitly labeled illustrative/non-normative; no non-English-sibling-file citation exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final phase-closing verification (NORM-08 and whole-diff coherence)</name>
  <read_first>
    docs/method/GG-SAD_normative_method_specification.md (post-Task-1/Task-2 state)
    .planning/phases/01-normative-clarification/01-BASELINE.txt (the pre-phase HEAD SHA committed by Plan 01-01's Task 1 — read it, never hard-code or recompute it)
    .planning/ROADMAP.md — Phase 1 success criteria
  </read_first>
  <files>docs/method/GG-SAD_normative_method_specification.md</files>
  <action>
    Run a single composite verification confirming the whole phase's diff is scoped and internally consistent before Phase 2 (Owner Approval) begins. Read the baseline SHA with `BASE=$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)` — this is the durable, non-hard-coded anchor Plan 01-01's Task 1 committed before any Phase 1 edit landed; it survives every intermediate per-task commit in Plans 01-01/01-02/01-03 because it is itself a committed file, read fresh in this task rather than assumed from a fixed SHA. Then confirm: (a) `git diff --name-only "$BASE"..HEAD`, filtered to drop any path under `.planning/` (canonical Phase 1 planning artifacts — SUMMARY.md files, ROADMAP.md updates, this baseline file itself — are expected to change and are not part of the product-scope check), contains exactly one remaining path, docs/method/GG-SAD_normative_method_specification.md; (b) that same baseline-anchored diff touches zero paths under src/ggsad/, zero paths under tests/, zero paths under .ggsad/schemas/, and zero paths matching the non-English sibling specification's filename suffix (the same suffix Task 2's Part C already checks for); (c) the top-level section count is 22 and every number 1 through 22 appears exactly once, in strictly ascending order, with no duplicate or out-of-order "## N." heading; (d) the Section 1.1 category-map table row count equals 22, matching the corrected top-level section count; (e) every subsection number under Sections 6, 7, 8, 9, 10, 11, 12, 13, and 22 matches its parent section number (re-run the specific greps used in this plan's Task 2 and Plan 01-02's Task 1 as a combined spot-check). This task makes no further content edits — it only verifies and reports. All commands run through the project's Bash tool per this plan's `<shell_contract>` (Git Bash / POSIX sh); none use `/tmp` or any other host-specific scratch path.
  </action>
  <verify>
    <automated>BASE=$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt); diff <(git diff --name-only "$BASE"..HEAD | grep -v '^\.planning/') <(echo "docs/method/GG-SAD_normative_method_specification.md")</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-normative-clarification/01-BASELINE.txt` exists and `BASE=$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)` resolves to a real commit: `git cat-file -e "$BASE"^{commit}` exits 0
    - Baseline-anchored, non-`.planning/` diff since `$BASE` lists exactly one path: `git diff --name-only "$BASE"..HEAD | grep -v '^\.planning/'` returns exactly `docs/method/GG-SAD_normative_method_specification.md`
    - `git diff --name-only "$BASE"..HEAD -- src/ggsad/` produces no output (empty)
    - `git diff --name-only "$BASE"..HEAD -- tests/` produces no output (empty)
    - `git diff --name-only "$BASE"..HEAD -- .ggsad/schemas/` produces no output (empty)
    - `git diff --name-only "$BASE"..HEAD | grep -c '_DE\.md$'` returns 0
    - `awk '/^## [0-9]+\. /{print}' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^## [0-9]+\.'` == 22
    - `awk '/^## [0-9]+\. /{gsub(/^## /,""); gsub(/\..*/,""); print}' docs/method/GG-SAD_normative_method_specification.md` printed in file order equals the sequence 1 through 22 with no gaps, duplicates, or reordering
    - `awk '/^### 1.1 Document Scope and Category Map/,/^## 2\. Normative Terms/' docs/method/GG-SAD_normative_method_specification.md | grep -cE '^\| [0-9]+'` == 22
  </acceptance_criteria>
  <done>All checks pass against the baseline-anchored diff (no hard-coded SHA): the phase's proposed diff is scoped to exactly the target file, allows only `.planning/` canonical artifacts alongside it, touches no path under src/ggsad/, tests/, or .ggsad/schemas/, and introduces no non-English-sibling-doc reference; section numbering and the category map are internally consistent, ready for Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> leading normative authority file | An AI agent directly edits the final wave of the leading document-authority file, including a wholly new normative section (22). |
| Ambiguous automation-contract envelope -> non-conforming implementations | A loosely specified envelope would let independently-built automation claim NORM-06 compatibility while returning different fields. |
| This phase's diff -> Phase 2/3 gates | Content produced across all of Phase 1's plans is not treated as final or mergeable until both hard gates complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Section 22 Minimal Result Envelope | high | mitigate | The six field names (operation, result, changed, state, issues, data) and their exact, non-uniform presence rules — operation/result/changed unconditional, state conditional on applicability, issues present for rejected/error and omitted on success, data always optional — are specified verbatim per D-04 (F-02), with the previously-proposed-then-narrowed fields explicitly excluded from the required set; acceptance criteria assert the field names, the presence-rule language, and the absence of the excluded snake_case names, scoped to the 22.2 subsection rather than a file-wide token grep. |
| T-03-02 | Tampering | Section 14.2 genericization | medium | mitigate | Task 2's action preserves the same five illustrative combinations and only replaces the product names, preventing accidental loss of the section's substantive content while removing vendor-specific citations. |
| T-03-03 | Tampering | src/ggsad/ (out-of-scope directory) | high | mitigate | Task 3 runs a baseline-anchored `git diff --name-only $(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)..HEAD` check across the whole phase (filtered to allow only `.planning/` canonical artifacts) and asserts zero paths under src/ggsad/, tests/, or .ggsad/schemas/ and no non-English-sibling-doc path, directly enforcing NORM-08 before the phase closes. |
| T-03-04 | Repudiation | git commit history for this file | medium | mitigate | The commit for this plan cites NORM-06/07/08 and D-04 explicitly, giving Phase 2/3 reviewers a traceable rationale record. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install tasks exist in this plan (documentation-only diff); the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `grep -c '^## 22\. Minimal Automation Contract' docs/method/GG-SAD_normative_method_specification.md` == 1
- `grep -cE '^### 22\.[123] ' docs/method/GG-SAD_normative_method_specification.md` == 3
- `grep -c 'Claude Code' docs/method/GG-SAD_normative_method_specification.md` == 0, `grep -c 'CodeRabbit' docs/method/GG-SAD_normative_method_specification.md` == 0
- `grep -cE '^## [0-9]+\.' docs/method/GG-SAD_normative_method_specification.md` == 22
- `BASE=$(cat .planning/phases/01-normative-clarification/01-BASELINE.txt)` resolves to a real commit (`git cat-file -e "$BASE"^{commit}` exits 0)
- Baseline-anchored, non-`.planning/` diff for the whole phase lists exactly one path: `git diff --name-only "$BASE"..HEAD | grep -v '^\.planning/'` returns exactly `docs/method/GG-SAD_normative_method_specification.md`
- No product path changed since `$BASE`: `git diff --name-only "$BASE"..HEAD -- src/ggsad/` is empty, `git diff --name-only "$BASE"..HEAD -- tests/` is empty, `git diff --name-only "$BASE"..HEAD -- .ggsad/schemas/` is empty, and `git diff --name-only "$BASE"..HEAD | grep -c '_DE\.md$'` returns 0
</verification>

<success_criteria>
- Section 22 defines the minimal automation contract's exact six field names and their precise, non-uniform presence rules — operation/result/changed unconditional, state conditional on applicability, issues present for rejected/error and omitted on success, data always optional (NORM-06 satisfied, F-02 resolved).
- Sections 6, 7, 9, 10, 11, 12, 13's subsection numbers match their parent section number; Section 14.2 no longer cites vendor tools and is explicitly labeled non-normative; no dangling non-English-sibling-file citation exists (NORM-07 satisfied).
- The whole Phase 1 diff, verified against the Plan 01-01-committed pre-phase baseline (not a bare or hard-coded `git diff`), touches exactly one product file and zero paths under src/ggsad/, tests/, .ggsad/schemas/, or any non-English-sibling doc — only `.planning/` canonical planning artifacts are allowed alongside it (NORM-08 satisfied, F-01 resolved).
- The Phase 1 diff is internally consistent (22 sequential top-level sections, a 22-row category map) and ready for Phase 2 Owner Approval.
</success_criteria>

<output>
Create `.planning/phases/01-normative-clarification/01-03-SUMMARY.md` when done
</output>
