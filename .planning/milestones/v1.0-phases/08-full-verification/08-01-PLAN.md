---
phase: 08-full-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-gap-remediation/07-RETROACTIVE-CLOSURE.md
  - .planning/phases/08-full-verification/08-VERIFICATION.md
  - .planning/phases/08-full-verification/08-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [VERIFY-01, VERIFY-02]
must_haves:
  truths:
    - "The exact six-command VERIFY-01 baseline passes under the documented UV_NATIVE_TLS=1 environment precondition without changing the required command strings."
    - "Every baseline result is recorded honestly, including the known pre-execution TLS diagnostic and any failed execution attempt."
    - "Phase 6 and Phase 7 metadata describes their retroactive evidence closure without inventing formal plans."
    - "Active requirement traceability agrees with completed requirement checkboxes and roadmap phase status."
    - "The English normative specification, product source, tests, configuration, .ggsad assets, .claude tooling, and archive remain unchanged."
    - "Milestone closure occurs only after explicit repository-owner authorization of the final verification result."
  artifacts:
    - ".planning/phases/07-gap-remediation/07-RETROACTIVE-CLOSURE.md — canonical explanation of Phase 7's evidence-only closure"
    - ".planning/phases/08-full-verification/08-VERIFICATION.md — exact VERIFY-01 and VERIFY-02 evidence"
    - ".planning/phases/08-full-verification/08-01-SUMMARY.md — Phase 8 outcome and scope summary"
    - ".planning/REQUIREMENTS.md — accurate completed requirement traceability"
    - ".planning/ROADMAP.md — accurate Phase 6, Phase 7, and Phase 8 plan/status history"
    - ".planning/STATE.md — current Phase 8 and milestone closure state"
  key_links:
    - "08-CONTEXT.md and TLS-INTERCEPTION-ROOT-CAUSE.md define the authoritative command sequence, native-TLS environment precondition, and fail-fast policy implemented by Task 2."
    - "06-RETROACTIVE-REVIEW.md and 07-RETROACTIVE-CLOSURE.md jointly explain why Phases 6 and 7 completed without formal plans."
    - "08-VERIFICATION.md maps the command evidence to VERIFY-01 and GSD evidence to VERIFY-02."
---

<objective>
Reconcile factual GSD metadata, run the complete Phase 8 verification baseline exactly as approved,
record reproducible requirement and GSD evidence, and stop at the explicit owner gate before closing
Milestone 1. Do not change method semantics, product behavior, tests, or quality-tool scope.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-implementation-conformance-audit/06-RETROACTIVE-REVIEW.md
@.planning/phases/08-full-verification/08-CONTEXT.md
@.planning/phases/08-full-verification/08-CONTEXT-REVIEW.md
@.planning/phases/08-full-verification/TLS-INTERCEPTION-ROOT-CAUSE.md
@.planning/milestones/PR-3-REBASE-SHA-RECONCILIATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reconcile Phase 6, Phase 7, and requirement metadata</name>
  <read_first>
    .planning/phases/06-implementation-conformance-audit/06-RETROACTIVE-REVIEW.md — owner-ratified
    evidence and the explicit treatment of Phases 6 and 7
    .planning/ROADMAP.md — Phase 6/7 details and Progress table
    .planning/REQUIREMENTS.md — completed checkboxes and stale Traceability statuses
    .planning/STATE.md — plan totals, current focus, continuity, and blockers
    .planning/phases/08-full-verification/08-CONTEXT-REVIEW.md — PCR-01 evidence
  </read_first>
  <files>
    .planning/phases/07-gap-remediation/07-RETROACTIVE-CLOSURE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>
    Preserve the historical fact established by `06-RETROACTIVE-REVIEW.md`: Phases 6 and 7 were
    ratified as substantively complete after the fact, with that review as their evidence record,
    and neither phase executed a formal GSD plan.

    Create `.planning/phases/07-gap-remediation/07-RETROACTIVE-CLOSURE.md`. State that Phase 7 had
    zero formal plans, that its gap-remediation outcome is evidenced by the owner-ratified
    `06-RETROACTIVE-REVIEW.md`, that this directory exists to make the completed phase visible to
    GSD without fabricating a plan, and that it does not retroactively waive normal planning or
    approval order for future work.

    In `.planning/ROADMAP.md`:

    - replace Phase 6's unresolved plan marker with `0 formal plans` and link its retroactive review;
    - replace Phase 7's unresolved plan marker with `0 formal plans` and link both evidence records;
    - change the Progress-table plan counts for Phases 6 and 7 from `1/1` to `0/0`, preserving their
      completion dates and Complete status;
    - leave Phase 8 not started until Task 2 succeeds and Task 3 records its evidence.

    In `.planning/REQUIREMENTS.md`, change only the Traceability statuses for NORM, AUDIT, and GAP
    from Pending to Complete. Preserve VERIFY as Pending and preserve all requirement wording and
    checked boxes.

    In `.planning/STATE.md`, preserve `total_plans: 7` and `completed_plans: 7` because those counts
    correctly represent the seven formal plans in Phases 1–5. Replace stale initialization and
    earlier-phase continuity/blocker text with current Phase 8 planning context. Do not mark Phase 8
    or the milestone complete.

    Run GSD consistency and health. Both must report zero errors. The missing-Phase-7 and `6 → 8`
    warnings must disappear after the new Phase 7 directory exists. Record any different warning
    for Task 3 rather than silently rewriting unrelated metadata.
  </action>
  <verify>
    <automated>node .claude\gsd-core\bin\gsd-tools.cjs validate consistency; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node .claude\gsd-core\bin\gsd-tools.cjs validate health; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n '0 formal plans|07-RETROACTIVE-CLOSURE' .planning/ROADMAP.md .planning/phases/07-gap-remediation/07-RETROACTIVE-CLOSURE.md; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - Phase 7 has a canonical directory and evidence-only closure record, but no invented plan.
    - ROADMAP and STATE agree that Phases 6 and 7 completed with zero formal plans.
    - Formal-plan totals remain seven before the Phase 8 plan is completed.
    - NORM, AUDIT, and GAP Traceability rows agree with their completed checkboxes.
    - VERIFY remains pending.
    - GSD no longer reports the absent Phase 7 directory or phase-numbering gap.
  </acceptance_criteria>
  <done>Active planning metadata truthfully represents the retroactive Phase 6/7 closure and current Phase 8 position.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the authoritative VERIFY-01 baseline with fail-fast evidence</name>
  <read_first>
    .planning/phases/08-full-verification/08-CONTEXT.md — Verification Baseline, Quality-Tool
    Boundary, Evidence Contract, and Completion Boundary
    .planning/phases/08-full-verification/08-CONTEXT-REVIEW.md — PCR-02 empirical TLS result
    .planning/phases/08-full-verification/TLS-INTERCEPTION-ROOT-CAUSE.md — Norton interception and
    uv trust-store mismatch evidence
    .planning/milestones/PR-3-REBASE-SHA-RECONCILIATION.md — prior TLS baseline condition
    AGENTS.md — repository engineering baseline
  </read_first>
  <files>
    .planning/phases/08-full-verification/08-VERIFICATION.md
  </files>
  <action>
    Before running commands, record in `08-VERIFICATION.md` that context review observed a cold
    `uv sync --locked` `UnknownIssuer` failure using uv's default bundled roots. Cite
    `TLS-INTERCEPTION-ROOT-CAUSE.md` and summarize its Norton interception and Windows trust-store
    evidence. Keep that pre-execution diagnostic separate from Phase 8's authoritative run.

    Set `$env:UV_NATIVE_TLS = '1'` in the process that will run all six commands. Record the variable
    name and value as `UV_NATIVE_TLS: 1` on its own line before the command-results table. Do not
    modify user- or machine-level
    environment settings and do not add uv configuration to the repository. Confirm the value with
    `$env:UV_NATIVE_TLS`; it must print `1` before `uv sync --locked` runs.

    Confirm the worktree has no changes outside the Task 1 planning files, the Phase 8 context and
    review, the Phase 8 plan, and `08-VERIFICATION.md`. Record the verified starting revision without
    making the evidence depend on the future commit containing itself.

    Run these commands sequentially, in this exact order, from the worktree root:

    1. `uv sync --locked`
    2. `uv run ruff format --check .`
    3. `uv run ruff check .`
    4. `uv run ty check src tests`
    5. `uv run pytest`
    6. `uv build`

    For each command, append its exact text, exit status, and material output to
    `08-VERIFICATION.md`. For pytest, record collected/passed counts and total coverage. For build,
    record the produced sdist and wheel paths. Use a six-row `VERIFY-01 Command Results` table with
    columns `Command`, `Exit status`, and `Result`; each successful row must contain numeric exit
    status `0` and result `PASS`.

    If any required command fails after `UV_NATIVE_TLS=1` is set, stop immediately. Record the
    failure verbatim, mark VERIFY-01 blocked, identify whether it is an environmental or repository
    failure, and do not run later required commands. Do not run a command-line `--native-tls` retry,
    change trust configuration, pre-warm dependencies, or continue to Ruff after a failed sync.
    Return control to the owner for disposition. Never convert a retry into a pass for the failed
    authoritative command.

    A blocker record must use this exact structure so the stop path is positively verifiable:

    - `VERIFY-01 status: BLOCKED` on its own line;
    - `Failed command:` followed by the exact command on its own line;
    - `Failure classification: ENVIRONMENTAL` or `Failure classification: REPOSITORY`;
    - `Later required commands run: No`;
    - one command-results row containing the exact failed command, its nonzero numeric exit status,
      and result `BLOCKED`;
    - a `### Failure output:` heading containing the exact command, followed by a nonempty fenced
      `text` block
      containing the command's verbatim output.

    Do not add command-results rows for any command after the failed command. This absence, together
    with `Later required commands run: No`, is evidence that fail-fast execution was honored.

    Bare `uv run ty check` is prohibited in this task. It is outside VERIFY-01 and would scan
    installer-owned `.claude/scripts/` content contrary to the Phase 5 ownership boundary.
  </action>
  <verify>
    <automated>$required = @('uv sync --locked', 'uv run ruff format --check .', 'uv run ruff check .', 'uv run ty check src tests', 'uv run pytest', 'uv build'); $evidence = Get-Content -Raw .planning/phases/08-full-verification/08-VERIFICATION.md; $environmentRecorded = $evidence -match '(?m)^UV_NATIVE_TLS:\s*1\s*$'; $passCount = 0; foreach ($command in $required) { $escaped = [regex]::Escape($command); if ($evidence -match "(?m)^\|\s*\x60?$escaped\x60?\s*\|\s*0\s*\|\s*PASS\s*\|") { $passCount++ } }; if ($environmentRecorded -and $passCount -eq 6) { exit 0 }; $failedMatch = [regex]::Match($evidence, '(?m)^Failed command:\s*(.+?)\s*$'); $failedCommand = if ($failedMatch.Success) { $failedMatch.Groups[1].Value } else { '' }; $failedIndex = [Array]::IndexOf($required, $failedCommand); $blocked = $environmentRecorded -and $failedIndex -ge 0 -and $evidence -match '(?m)^VERIFY-01 status:\s*BLOCKED\s*$' -and $evidence -match '(?m)^Failure classification:\s*(ENVIRONMENTAL|REPOSITORY)\s*$' -and $evidence -match '(?m)^Later required commands run:\s*No\s*$'; if ($blocked) { $escapedFailed = [regex]::Escape($failedCommand); $blocked = $evidence -match "(?m)^\|\s*\x60?$escapedFailed\x60?\s*\|\s*[1-9][0-9]*\s*\|\s*BLOCKED\s*\|" -and $evidence -match "(?s)### Failure output:\s*\x60?$escapedFailed\x60?.*?\x60{3}text\s+\S.*?\x60{3}" }; $laterRows = $false; if ($failedIndex -ge 0 -and $failedIndex -lt ($required.Count - 1)) { foreach ($command in $required[($failedIndex + 1)..($required.Count - 1)]) { $escaped = [regex]::Escape($command); if ($evidence -match "(?m)^\|\s*\x60?$escaped\x60?\s*\|") { $laterRows = $true } } }; if ($blocked -and -not $laterRows -and $passCount -eq $failedIndex) { exit 0 }; Write-Error 'Evidence is neither six clean PASS rows nor a complete fail-fast blocker record'; exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `UV_NATIVE_TLS=1` is recorded and active before the authoritative run.
    - The six authoritative command strings run unchanged, in order, and each exits zero.
    - No command-line native-TLS variant or post-failure retry substitutes for a required command.
    - The pre-execution diagnostic and Phase 8 execution results are clearly separated.
    - Every command has reproducible evidence, including pytest and build result details.
    - A failure stops execution and leaves VERIFY-01 incomplete.
  </acceptance_criteria>
  <done>The exact VERIFY-01 baseline passes with complete, honest evidence, or Phase 8 stops with a precise blocker record.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record GSD verification and prepare the owner closure gate</name>
  <read_first>
    .planning/phases/08-full-verification/08-VERIFICATION.md — successful Task 2 evidence
    .planning/phases/08-full-verification/08-CONTEXT.md — Evidence Contract and Scope Boundaries
    .planning/REQUIREMENTS.md — VERIFY-01 and VERIFY-02
    .planning/ROADMAP.md — Phase 8 status
    .planning/STATE.md — current Phase 8 state
  </read_first>
  <files>
    .planning/phases/08-full-verification/08-VERIFICATION.md
    .planning/phases/08-full-verification/08-01-SUMMARY.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
    Execute this task only if all six Task 2 commands exited zero. Otherwise stop without creating a
    success summary or changing VERIFY statuses.

    Run supported GSD consistency and health validations and append their complete structured
    results to `08-VERIFICATION.md`. Run `git diff --check`. Compare the Phase 8 diff to its starting
    revision and confirm there are no changes in:

    - `docs/method/GG-SAD_normative_method_specification.md`;
    - `src/ggsad/`;
    - `tests/`;
    - `pyproject.toml`, `uv.lock`, or other product configuration;
    - `.ggsad/`;
    - `.claude/`;
    - `archive/`.

    Record a VERIFY-01/VERIFY-02 requirements table in `08-VERIFICATION.md`, with exact evidence
    references for every success criterion. Record residual GSD warnings individually with cause,
    disposition, and closure impact. The absent Phase 7 and numbering-gap warnings must not remain.

    Create `08-01-SUMMARY.md` describing the metadata reconciliation, command results, protected
    scope, known pre-execution TLS diagnostic, and the pending owner gate. Mark VERIFY-01 and
    VERIFY-02 Complete in `.planning/REQUIREMENTS.md` only after all evidence checks pass.

    Update `.planning/ROADMAP.md` and `.planning/STATE.md` to show Phase 8 verification complete but
    milestone closure awaiting explicit owner authorization. Count `08-01-PLAN.md` as the eighth
    formal completed plan. Do not create a tag, mark the milestone closed, start a new milestone, or
    push/merge anything in this task.
  </action>
  <verify>
    <automated>node .claude\gsd-core\bin\gsd-tools.cjs validate consistency; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node .claude\gsd-core\bin\gsd-tools.cjs validate health; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --exit-code 0824da144a8366a758f05d016f03c98c43d46ed3 -- docs/method/GG-SAD_normative_method_specification.md src tests pyproject.toml uv.lock .ggsad .claude archive</automated>
  </verify>
  <acceptance_criteria>
    - VERIFY-01 and VERIFY-02 each have exact evidence and are marked Complete.
    - GSD consistency and health have zero errors; every warning is explicitly dispositioned.
    - The Phase 8 summary states that milestone closure still requires owner authorization.
    - Formal plan totals become eight only after this plan's evidence is complete.
    - Protected method, product, test, configuration, asset, installer, and archive scopes are unchanged.
  </acceptance_criteria>
  <done>Phase 8 is verified and documented, with Milestone 1 waiting at the explicit owner closure gate.</done>
</task>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 4: Obtain explicit owner authorization for Milestone 1 closure</name>
  <read_first>
    .planning/phases/08-full-verification/08-VERIFICATION.md
    .planning/phases/08-full-verification/08-01-SUMMARY.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
    Present the exact Phase 8 verification result, GSD validation result, scope confirmation, and
    any warnings to the repository owner. Ask for explicit authorization to close Milestone 1.

    Stop and wait. Approval of this plan or approval to execute Phase 8 is not milestone-closure
    authorization. Only an explicit response after the owner reviews the completed verification
    evidence satisfies this checkpoint.

    If the owner approves, use the supported pinned-GSD milestone completion workflow to update
    final roadmap/state fields and create the configured milestone tag. Show the exact proposed tag
    before creating it. If the owner requests changes or declines closure, record the requested
    disposition and leave the milestone open. Do not infer approval from silence or prior approvals.
  </action>
  <verify>
    <manual>Repository owner explicitly approves or declines Milestone 1 closure after reviewing the completed Phase 8 evidence.</manual>
  </verify>
  <acceptance_criteria>
    - The owner sees the complete Phase 8 result before deciding.
    - Milestone closure and tag creation occur only after explicit authorization.
    - A declined or conditional decision leaves the milestone open and records the disposition.
  </acceptance_criteria>
  <done>The owner has explicitly dispositioned Milestone 1 closure; any authorized GSD closure and tag are recorded.</done>
</task>

</tasks>

<verification>
- Phase 6 and Phase 7 completion history is consistent without invented plans.
- The exact six-command VERIFY-01 baseline runs under `UV_NATIVE_TLS=1` and passes or execution stops at the first failure.
- VERIFY-01 and VERIFY-02 have reproducible GSD evidence.
- Protected method, product, test, configuration, asset, installer, and archive scopes are unchanged.
- Phase 8 and Milestone 1 remain separate gates; owner authorization controls final closure.
</verification>

<output>
Create `08-VERIFICATION.md` during Task 2. Create `08-01-SUMMARY.md` and mark VERIFY requirements
complete only after all six authoritative commands and the Task 3 scope/GSD checks pass. Stop at
Task 4 until the repository owner explicitly dispositions milestone closure.
</output>
