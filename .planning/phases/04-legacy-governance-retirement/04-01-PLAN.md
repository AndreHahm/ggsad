---
phase: 04-legacy-governance-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - archive/legacy-ggsad-governance/MANIFEST.md
  - archive/legacy-ggsad-governance/docs/**
  - archive/legacy-ggsad-governance/specs/CHG-001-reference-repository-bootstrap/**
  - README.md
  - tests/fixtures/governed_artifacts/representative-state.yaml
  - tests/integration/test_governed_artifact_validation.py
autonomous: false
requirements: [RETIRE-01, RETIRE-02, RETIRE-03, RETIRE-04, RETIRE-05]
must_haves:
  truths:
    - "All 28 legacy files are moved byte-identically beneath the historical archive and the already-absent startup file is dispositioned."
    - "Both architecture documents and both roadmap documents have explicit, non-competing historical classifications."
    - "README describes the current English-specification and GSD authority model without active CHG-001 or subordinate-GSD framing."
    - "Active tests use dedicated current-schema fixture data rather than archived CHG-001 evidence."
    - "The English normative specification, product implementation, .ggsad assets, and unrelated tests remain unchanged."
  artifacts:
    - "archive/legacy-ggsad-governance/MANIFEST.md — complete 29-disposition historical index"
    - "README.md — current non-normative repository entry point"
    - "tests/fixtures/governed_artifacts/representative-state.yaml — dedicated schema/model fixture"
    - ".planning/phases/04-legacy-governance-retirement/04-VERIFICATION.md — archive and scope evidence"
  key_links:
    - "Every manifest destination maps to an inventory entry in 04-CONTEXT.md."
    - "README points to AGENTS.md and .planning/ instead of archived governance."
    - "The integration test reads only the dedicated fixture, never archive/ or specs/CHG-001."
---

<objective>
Archive the complete superseded GG-SAD repository-governance corpus without altering historical
content, rewrite the active README to the current authority model, decouple active tests from
CHG-001 evidence, and verify RETIRE-01 through RETIRE-05. Phase 5 is out of scope and must not begin
automatically.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-legacy-governance-retirement/04-CONTEXT.md
@.planning/phases/04-legacy-governance-retirement/04-CONTEXT-REVIEW.md
@.planning/intel/context.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decouple state validation tests from CHG-001 evidence</name>
  <read_first>
    tests/integration/test_governed_artifact_validation.py
    .ggsad/schemas/state.schema.json
    src/ggsad/models/state.py
    .planning/phases/04-legacy-governance-retirement/04-CONTEXT.md — Test-Fixture Decoupling
  </read_first>
  <files>
    tests/fixtures/governed_artifacts/representative-state.yaml
    tests/integration/test_governed_artifact_validation.py
  </files>
  <action>
    First change the integration module to use a constant
    `STATE_FIXTURE_PATH = REPO_ROOT / "tests" / "fixtures" / "governed_artifacts" /
    "representative-state.yaml"`. Rename `test_chg_001_state_is_schema_and_model_valid` to
    `test_representative_state_is_schema_and_model_valid`, remove CHG-001/slice assertions and
    comments, and assert fixture identity `CHG-900`, phase `specify`, status `ready`, and one history
    event. Make `test_prf005_state_schema_rejects_unsupported_schema_version` read the same constant.
    Rewrite the module docstring to say the module validates the repository's active configuration,
    mapping, and representative state fixture; it must not name CHG-001.

    Run the renamed test before creating the fixture and confirm it fails with a missing-file error.
    Then create the fixture with exactly this schema-valid content:

    ```yaml
    schema_version: "0.1"
    change:
      id: CHG-900
      slug: representative-state
      title: Representative State Fixture
      class: M
    flow:
      profile: standard
      phase: specify
      status: ready
    goal:
      summary: Validate a representative persistent state artifact.
    artifacts:
      spec: spec.md
      plan: null
      tasks: null
      evidence: null
    pair_review:
      required: false
      status: not_required
      requestor: null
      reviewer: null
    wait:
      reason: null
      category: null
      owner: null
      resume_condition: null
      safe_state: null
      resume_phase: null
      next_action: null
    failure:
      reason: null
      category: null
    history:
      - timestamp: "2026-08-20T00:00:00Z"
        event: fixture-created
        actor: test-suite
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_governed_artifact_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The renamed test fails before fixture creation and passes afterward.
    - The module contains no `CHG-001`, `chg_001`, `Slice`, or `specs/CHG-001` text.
    - Both state tests read `STATE_FIXTURE_PATH`.
    - The fixture passes the current JSON Schema and Pydantic model.
    - No product implementation or schema is changed.
  </acceptance_criteria>
  <done>Active state regression coverage is independent from historical CHG-001 evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move all legacy artifacts into the historical archive and create the manifest</name>
  <read_first>
    .planning/phases/04-legacy-governance-retirement/04-CONTEXT.md — Archive Inventory and Manifest Contract
    .planning/intel/context.md
    every source path listed in the 04-CONTEXT.md Archive Inventory
  </read_first>
  <files>
    archive/legacy-ggsad-governance/MANIFEST.md
    archive/legacy-ggsad-governance/docs/**
    archive/legacy-ggsad-governance/specs/CHG-001-reference-repository-bootstrap/**
  </files>
  <action>
    Resolve and verify each of the 28 source files before moving anything. Create the archive root,
    then move files with exact source and destination paths while preserving the subtree beneath
    `archive/legacy-ggsad-governance/`. Use native PowerShell `Move-Item -LiteralPath` or `git mv`
    end-to-end; do not enumerate paths in one shell and move them through another. Move:

    - the eight individual root-level legacy docs named in 04-CONTEXT (`constitution`,
      `project-brief`, both architecture files, both roadmaps, implementation guide, workflow
      reference);
    - all eight `docs/adr/ADR-0001` through `ADR-0008` files;
    - all four `docs/definitions/` files;
    - both `docs/guides/` files;
    - `docs/method/GG-SAD_normative_method_specification_DE.md`;
    - all five files beneath `specs/CHG-001-reference-repository-bootstrap/`.

    Do not move the English normative specification or any other file. Remove now-empty source
    directories only when Git no longer tracks content in them.

    Create `MANIFEST.md` with a non-authoritative archive warning, current-authority pointers, and
    one row for every one of the 29 inventory entries. Each row must contain original path, archived
    path (or `Absent before Phase 4` for `CLAUDE_CODE_PROJECT_START.md`), classification, rationale,
    original status where relevant, and current authority. Use the exact classifications and
    treatments from 04-CONTEXT. Add explicit notes that both architectures were retired without a
    replacement, `docs/roadmap.md` is the historical delivery record while
    `docs/implementation-roadmap.md` was aspirational, all ADRs remained Proposed, and CHG-001 is
    not conformance proof.
  </action>
  <verify>
    <automated>git diff --name-status --find-renames=100% HEAD -- archive docs specs</automated>
  </verify>
  <acceptance_criteria>
    - Git reports exactly 28 `R100` moves for inventoried historical files.
    - The manifest contains exactly 29 disposition rows and every archive destination exists.
    - All 28 original active paths are absent; `CLAUDE_CODE_PROJECT_START.md` remains absent.
    - No moved historical file has content changes; every move is detected at 100% similarity.
    - The English normative specification remains at its original active path and is byte-unchanged.
  </acceptance_criteria>
  <done>The complete legacy corpus is preserved outside active governance with an auditable manifest.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite README as the current repository entry point</name>
  <read_first>
    README.md
    AGENTS.md
    CLAUDE.md
    .planning/PROJECT.md
    .planning/ROADMAP.md
    archive/legacy-ggsad-governance/MANIFEST.md
    docs/method/GG-SAD_normative_method_specification.md — Purpose and Document Scope only
  </read_first>
  <files>README.md</files>
  <action>
    Rewrite README rather than performing isolated reference deletion. Preserve accurate product
    introduction, capability summary, CLI quick start, configuration/schema paths, templates,
    security, compatibility, and license information. Replace the old Operating Modes/GSD
    Integration authority narrative with a Repository Authority and Development Method section that
    points to the English normative specification for product semantics, `AGENTS.md` for repository
    instructions, and `.planning/` for GSD Core 1.10.0 development state.

    Replace the Document Hierarchy section with a short authority-pointer section; do not reproduce
    a competing precedence list. Replace Project Status and Initial Scope with Current Development
    Status that points to `.planning/ROADMAP.md` and does not restate CHG-001 delivery claims.
    Replace the repository tree so it shows `.planning/`, the active English specification,
    `archive/legacy-ggsad-governance/`, `.ggsad/`, `src/`, and `tests/`, without showing archived
    paths as active. Update Quick Start to `uv sync --locked`. Remove the unpinned GSD installation
    command and greenfield onboarding instructions; the repository already pins GSD Core 1.10.0.
    Replace the Development Workflow and Contributing instructions with references to `AGENTS.md`,
    `CLAUDE.md`, and active `.planning/` artifacts. Link the archive manifest once under a clearly
    historical heading.
  </action>
  <verify>
    <automated>rg -n 'docs/(?:constitution|project-brief|architecture|architecture-reference|roadmap|implementation-roadmap|implementation-guide|workflow-reference)\.md|docs/(?:adr|definitions|guides)/|GG-SAD_normative_method_specification_DE\.md|specs/CHG-001|CLAUDE_CODE_PROJECT_START\.md|GSD artifacts.*subordinate|GSD does not own|@opengsd/gsd-core@latest' README.md AGENTS.md CLAUDE.md THIRD_PARTY_NOTICES.md</automated>
  </verify>
  <acceptance_criteria>
    - The prohibited-reference scan returns no matches.
    - README names the English normative specification, `AGENTS.md`, GSD Core 1.10.0, and `.planning/` in their current roles.
    - README links `archive/legacy-ggsad-governance/MANIFEST.md` only as historical context.
    - README uses `uv sync --locked` and contains no unpinned GSD installation command.
    - Product usage sections remain non-normative and materially complete.
  </acceptance_criteria>
  <done>README no longer exposes superseded repository governance as active guidance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify retirement scope, requirements, and baseline</name>
  <read_first>
    .planning/phases/04-legacy-governance-retirement/04-CONTEXT.md
    .planning/phases/04-legacy-governance-retirement/04-CONTEXT-REVIEW.md
    archive/legacy-ggsad-governance/MANIFEST.md
    README.md
    tests/integration/test_governed_artifact_validation.py
  </read_first>
  <files>
    .planning/phases/04-legacy-governance-retirement/04-01-SUMMARY.md
    .planning/phases/04-legacy-governance-retirement/04-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Verify every 04-CONTEXT Verification Contract item. Confirm the Phase 4 non-planning diff is
    limited to 28 exact moves, the manifest, README, the state fixture, and the one integration test.
    Compare the English normative specification, `src/ggsad/`, `.ggsad/`, and unrelated tests to the
    Phase 4 starting revision `677b6f0`; they must be unchanged. Scan active files while excluding
    `archive/`, `.planning/`, installer-owned `.claude/`, and historical `docs/superpowers/` records;
    no active repository-governance instruction may point to an archived path. Repeat Task 3's full
    archived-path scan against `README.md`, `AGENTS.md`, `CLAUDE.md`, and
    `THIRD_PARTY_NOTICES.md`; zero matches are required. Run GSD consistency and the full applicable
    baseline. Record the known installer-owned `ty` diagnostics separately; do not weaken
    configuration in Phase 4.

    Mark RETIRE-01 through RETIRE-05 complete only after all scope and archive checks pass. Create
    summary and verification reports with exact commands and results. Stop after Phase 4 completion;
    do not start Phase 5 automatically.
  </action>
  <verify>
    <automated>! rg -n 'docs/(?:constitution|project-brief|architecture|architecture-reference|roadmap|implementation-roadmap|implementation-guide|workflow-reference)\.md|docs/(?:adr|definitions|guides)/|GG-SAD_normative_method_specification_DE\.md|specs/CHG-001|CLAUDE_CODE_PROJECT_START\.md|GSD artifacts.*subordinate|GSD does not own|@opengsd/gsd-core@latest' README.md AGENTS.md CLAUDE.md THIRD_PARTY_NOTICES.md &amp;&amp; uv sync --locked &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run pytest &amp;&amp; uv build</automated>
  </verify>
  <acceptance_criteria>
    - RETIRE-01 through RETIRE-05 each have explicit evidence.
    - All archive, manifest, active-reference, test-decoupling, and protected-scope checks pass.
    - Ruff, pytest, and packaging pass; `ty` output is reported without hiding installer-owned diagnostics.
    - GSD verification passes and Phase 4 closure artifacts contain no placeholders.
    - Phase 5 remains unstarted.
  </acceptance_criteria>
  <done>Legacy governance is retired with preserved history, clean active guidance, passing product checks, and auditable evidence.</done>
</task>

</tasks>

<verification>
- 29 dispositions are complete: 28 exact archive moves and one already-absent startup file.
- No legacy artifact remains a plausible active repository-development authority.
- Historical CHG-001 evidence is preserved but unused by active tests or conformance claims.
- Current README and agent instructions consistently point to the English normative specification and GSD planning.
- Protected product and normative scopes are unchanged.
</verification>

<output>
Create `04-01-SUMMARY.md` and `04-VERIFICATION.md` only after all four tasks and the full scope check pass.
</output>
