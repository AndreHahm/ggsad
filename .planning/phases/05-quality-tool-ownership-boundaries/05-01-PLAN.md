---
phase: 05-quality-tool-ownership-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - AGENTS.md
  - README.md
  - specs/examples/class-m/evidence.md
autonomous: false
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]
must_haves:
  truths:
    - "Ruff keeps its configuration-backed repository-root boundary, excludes installer-owned .claude content, and does not exclude future owned scripts."
    - "Strict ty checking passes for the explicit owned paths src and tests without scanning installer-owned .claude scripts."
    - "Every retained active baseline uses uv sync --locked and uv run ty check src tests."
    - "pytest discovery and coverage remain scoped to tests and the ggsad package without placeholder script configuration."
    - "The English normative specification, product implementation, tests, .ggsad assets, installer-owned .claude files, and archive remain unchanged."
  artifacts:
    - "pyproject.toml — configuration-backed Ruff, strict ty, pytest, and coverage boundary"
    - "AGENTS.md — canonical repository engineering baseline"
    - "README.md — matching contributor-facing baseline"
    - "specs/examples/class-m/evidence.md — matching active illustrative evidence commands"
    - ".planning/phases/05-quality-tool-ownership-boundaries/05-VERIFICATION.md — requirement and scope evidence"
  key_links:
    - "AGENTS.md, README.md, and the active Class M example use the same six baseline commands."
    - "The ty command's src tests arguments implement the owned-source allowlist defined in 05-CONTEXT.md."
    - "Ruff's .claude exclusion separates installer tooling while removal of scripts preserves future owned-script coverage."
---

<objective>
Establish explicit product quality-tool ownership using Ruff's configuration-backed exclusions,
`ty`'s explicit owned paths, existing pytest/coverage configuration, and one canonical documented
baseline. Do not add wrappers, policy tests, placeholder directories, or product changes.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT.md
@.planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT-REVIEW.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Align quality-tool configuration with owned Python scope</name>
  <read_first>
    pyproject.toml — `[tool.ruff]`, `[tool.ty.*]`, `[tool.pytest.ini_options]`, and
    `[tool.coverage.*]`
    .planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT.md — Ownership Boundary and
    Tool-Specific Scope
    .planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT-REVIEW.md — empirical command
    results
  </read_first>
  <files>pyproject.toml</files>
  <action>
    Capture the pre-change boundary by running
    `rg -n '^\s*"(?:\.claude|scripts)",$' pyproject.toml`; it must show both `.claude` and
    `scripts` in Ruff's `extend-exclude` list. Remove only the exact `"scripts",` entry. Keep
    `.claude` and every other existing exclusion unchanged. Do not change Ruff commands, lint rules,
    strict `ty` rules, pytest discovery, coverage source, thresholds, or any dependency.

    Verify the resulting tool contracts directly:

    - `rg -n '^\s*"\.claude",$' pyproject.toml` returns exactly the `.claude` exclusion;
    - `rg -n '^\s*"scripts",$' pyproject.toml` returns no match;
    - `uv run ruff format --check .` passes;
    - `uv run ruff check .` passes;
    - `uv run ty check src tests` passes with zero diagnostics;
    - `uv run pytest` passes with the configured coverage gate.

    The absence of a current root `scripts/` directory is intentional. Do not create one and do not
    add a nonexistent coverage source.
  </action>
  <verify>
    <automated>uv run ruff format --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run ty check src tests &amp;&amp; uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - Ruff still uses repository-root commands with configuration-backed exclusions.
    - `.claude` remains in `extend-exclude`; `scripts` does not.
    - `[tool.ty.rules] all = "error"` is unchanged.
    - Scoped strict `ty`, Ruff, and pytest pass.
    - No wrapper, placeholder scripts directory, dependency, product source, or test change exists.
  </acceptance_criteria>
  <done>The configured quality boundary excludes installer tooling without excluding future owned scripts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Synchronize retained active baseline commands</name>
  <read_first>
    AGENTS.md — Engineering baseline
    README.md — Requirements and Quick Start
    specs/examples/class-m/evidence.md — Environment and Tooling Evidence table
    .planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT.md — Canonical Baseline
  </read_first>
  <files>
    AGENTS.md
    README.md
    specs/examples/class-m/evidence.md
  </files>
  <action>
    Before editing, run these drift probes against the three files:

    - `rg -n --pcre2 'uv sync(?! --locked)' AGENTS.md README.md specs/examples/class-m/evidence.md`
      must identify the Class M example's bare sync command;
    - `rg -n --pcre2 'uv run ty check(?! src tests)' AGENTS.md README.md specs/examples/class-m/evidence.md`
      must identify all three obsolete type-check commands.

    Make exactly these command substitutions:

    - in `AGENTS.md`, replace `uv run ty check` with `uv run ty check src tests`;
    - in README, replace `uv run ty check` with `uv run ty check src tests`;
    - in `specs/examples/class-m/evidence.md`, replace `uv sync` with `uv sync --locked` and
      `uv run ty check` with `uv run ty check src tests`.

    Preserve the Ruff repository-root commands, pytest command, build command, evidence statuses,
    prose, and table structure. Do not add commands to `CLAUDE.md`, which already delegates to
    `AGENTS.md`. Do not edit archived copies or the English normative specification.

    After editing, repeat both drift probes; each must return no matches. Run
    `rg -n 'mypy|pyright' AGENTS.md README.md specs/examples/class-m/evidence.md CLAUDE.md`; it must
    return no matches. Confirm each of the three edited files contains
    `uv run ty check src tests`, and each dependency-sync command in those files is locked.
  </action>
  <verify>
    <automated>rg -n --pcre2 'uv sync(?! --locked)|uv run ty check(?! src tests)' AGENTS.md README.md specs/examples/class-m/evidence.md; if ($LASTEXITCODE -ne 1) { exit 1 }; rg -n 'mypy|pyright' AGENTS.md README.md specs/examples/class-m/evidence.md CLAUDE.md; if ($LASTEXITCODE -ne 1) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - All three active command surfaces use `uv run ty check src tests`.
    - All dependency-sync commands in those files use `uv sync --locked`.
    - Ruff remains `uv run ruff format --check .` and `uv run ruff check .`.
    - No `mypy` or `pyright` baseline is introduced.
    - `CLAUDE.md`, archives, product code, and tests remain unchanged.
  </acceptance_criteria>
  <done>Retained active instructions and examples teach one locked, ownership-scoped baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify ownership scope, requirements, and baseline</name>
  <read_first>
    .planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT.md
    .planning/phases/05-quality-tool-ownership-boundaries/05-CONTEXT-REVIEW.md
    pyproject.toml
    AGENTS.md
    README.md
    specs/examples/class-m/evidence.md
  </read_first>
  <files>
    .planning/phases/05-quality-tool-ownership-boundaries/05-01-SUMMARY.md
    .planning/phases/05-quality-tool-ownership-boundaries/05-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Verify every 05-CONTEXT Verification Contract item. Compare non-planning changes to the Phase 5
    starting revision `6f98eab`; they must be limited to `pyproject.toml`, `AGENTS.md`, `README.md`,
    and `specs/examples/class-m/evidence.md`. Confirm no differences in the English normative
    specification, `src/ggsad/`, `tests/`, `.ggsad/`, `.claude/`, or `archive/`.

    Run the configuration assertions and retained-document drift scans from Tasks 1 and 2. Run
    `uv sync --locked`, Ruff formatting and linting with repository-root commands,
    `uv run ty check src tests`, the full pytest suite, `uv build`, and GSD consistency. If plain
    `uv build` fails with the confirmed `UnknownIssuer` environment condition, record the failure
    verbatim and run `uv build --native-tls`; the fallback must pass before Phase 5 can close.

    Mark TOOL-01 through TOOL-04 complete only after every ownership, drift, protected-scope, and
    applicable baseline check passes. Create the summary and verification reports with exact
    commands and results. Record that no wrapper, policy test, placeholder directory, or quality
    suppression was added. Stop after Phase 5 completion; do not create a Phase 6 directory or
    begin its audit.
  </action>
  <verify>
    <automated>uv sync --locked &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ruff check . &amp;&amp; uv run ty check src tests &amp;&amp; uv run pytest &amp;&amp; uv build --native-tls</automated>
  </verify>
  <acceptance_criteria>
    - TOOL-01 through TOOL-04 each have explicit evidence.
    - Configuration, command-drift, and protected-scope checks pass.
    - Ruff, scoped strict `ty`, pytest, and packaging pass, with any certificate fallback reported honestly.
    - GSD consistency passes and Phase 5 closure artifacts contain no placeholders.
    - Phase 6 remains unstarted.
  </acceptance_criteria>
  <done>Product quality-tool ownership is explicit, consistently documented, and fully verified.</done>
</task>

</tasks>

<verification>
- Ruff excludes installer-owned `.claude` content but permits future owned scripts.
- Strict `ty` checks only `src` and `tests` and passes without suppression.
- Active retained command surfaces agree on locked sync and scoped type checking.
- pytest and coverage retain their existing owned product/test boundary.
- Protected normative, product, test, installer, and archive scopes are unchanged.
</verification>

<output>
Create `05-01-SUMMARY.md` and `05-VERIFICATION.md` only after all three tasks and the full scope check pass.
</output>
